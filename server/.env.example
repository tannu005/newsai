# Google Gemini AI Config
GOOGLE_API_KEY=your_gemini_api_key_here
PORT=3001

# Inngest Config (for local development)
INNGEST_DEV=1
# INNGEST_SIGNING_KEY= (only needed for production)

# Vector Store Config
# MONGODB_URI= (optional, falls back to local JSON for history)

# Frontend URL (for CORS)
VITE_API_URL=http://localhost:3001
